<p>These are example applications for Weblocks.
Originally these examples were in Weblocks repository and then moved out.</p>

<h1>Weblocks Demo</h1>

<p>A simple Weblocks demo, contains two editable related tables.</p>

<ul>
<li><code>(ql:quickload :weblocks-demo)</code></li>
<li><code>(weblocks-demo:start-weblocks-demo)</code></li>
<li>Go to http://localhost:8080/weblocks-demo url</li>
</ul>

<h1>Weblocks CLSQL Demo</h1>

<p>Similar to &ldquo;Weblocks Demo&rdquo;, but uses CLSQL as database layer.</p>

<ul>
<li><code>(ql:quickload :weblocks-clsql-demo)</code></li>
<li>Put correct auth data for mysql into weblocks-clsql-demo/conf/stores.lisp 
  (for quicklisp usually .quicklisp/dists/quicklisp/software/weblocks-examples-&hellip;/weblocks-clsql-demo/conf/stores.lisp)
  There should be just access to empty database.</li>
<li>Again <code>(ql:quickload :weblocks-clsql-demo)</code></li>
<li><code>(weblocks-clsql-demo:start-weblocks-clsql-demo)</code></li>
<li>Go to http://localhost:8080/ url</li>
</ul>

<h1>Weblocks Elephant Demo</h1>

<p>Similar to &ldquo;Weblocks Demo&rdquo;, but uses Elephant as database layer.</p>

<ul>
<li><code>(ql:quickload :weblocks-elephant-demo)</code></li>
<li>Install Berkeley DB. For me it worked with libdb4.6 and libdb4.6-dev</li>
<li>Update elephant config. It is usually situated in .quicklisp/dists/quicklisp/software/elephant-&hellip;/my-config.sexp 
  I&apos;ve changed first 4 strings of my-config.sexp to</li>
</ul>

<p><code>lisp
  (:COMPILER . :GCC) (:BERKELEY-DB-VERSION . &quot;4.6&quot;)
  (:BERKELEY-DB-INCLUDE-DIR . &quot;/usr/local/BerkeleyDB.4.6/include/&quot;)
  (:BERKELEY-DB-LIB-DIR . &quot;/usr/local/BerkeleyDB.4.6/lib/&quot;)
  (:BERKELEY-DB-LIB . &quot;/usr/local/BerkeleyDB.4.6/lib/libdb-4.6.so&quot;)
</code></p>

<ul>
<li>Create weblocks-elephant-demo/data (for quicklisp usually .quicklisp/dists/quicklisp/software/weblocks-examples-&hellip;/weblocks-elephant-demo/data)</li>
<li><code>(weblocks-elephant-demo:start-weblocks-elephant-demo)</code></li>
<li>Go to http://localhost:8080/weblocks-demo url</li>
</ul>

<h1>Simple Blog</h1>

<p>A simple blog, contains two editable related tables with users, posts (admin interface), frontend for posts and Slava Akhmechet photo.</p>

<ul>
<li><code>(ql:quickload :simple-blog)</code></li>
<li><code>(simple-blog:start-simple-blog)</code></li>
<li>Go to http://localhost:8080/ url</li>
</ul>
